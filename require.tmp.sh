#!/usr/bin/env bash
#/ Usage: (included script)
#/     require array.class whatever.class "http://whatever.class" && (
#/        stuff
#/     )     # but that is just silly, because bash is not event based.
